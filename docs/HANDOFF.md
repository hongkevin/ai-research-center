# 핸드오프 — 2026-08-04

다른 세션에서 이어받기 위한 문서입니다. **먼저 이걸 읽고, 그다음
[decisions.md](decisions.md)를 읽으십시오.** 결정의 이유는 전부 거기 있습니다.

---

## 이 제품이 무엇인가

**코스닥 미커버 종목의 실적 리뷰 노트를 반자동으로 만든다.** AI가 초안을 쓰고
사람이 검토해 발간한다. 상장사의 약 70%가 애널리스트 커버리지가 없고, 그중
코스닥 소형주가 가장 비어 있다.

핵심 차별점은 글이 아니라 **검증**이다:

> 리포트에 등장하는 모든 숫자는 결정적 코드가 계산해 레지스트리에 등록한 것이고,
> 클릭하면 DART 공시 원문까지 되짚을 수 있다. LLM은 숫자를 **볼 수도 쓸 수도** 없다.

이게 ChatGPT로 대체 불가능한 유일한 지점이다. 여기가 무너지면 제품이 없다.

---

## 절대 깨면 안 되는 불변식 일곱

새 기능을 넣을 때 이 일곱을 먼저 확인하십시오. 개발 중 게이트가 **여섯 번** 잡았고
전부 진짜 결함이었습니다.

### 1. 숫자는 레지스트리를 거치고, **출처는 항목마다 다르다**

LLM은 `{{num:key}}` 플레이스홀더만 쓴다. 값은 프롬프트에 넣지 않는다
(`llm/number_registry.py`, `verify/g0.py`).

**새 수치를 본문에 넣을 때는 반드시 `NumberEntry`로 등록하십시오.** 지분율·가정·
부문 매출 모두 여기서 걸렸습니다. 리터럴로 쓰면 G0가 발간을 막습니다.

공시 원문을 프롬프트에 넣을 때는 `mask_numbers()`로 가립니다 — 탐지와 **같은
화이트리스트**를 쓰므로 규칙이 갈라지지 않습니다.

**출처를 하나로 뭉치지 마십시오** (D36). 배당성향은 `alotMatter`, 지분은
`hyslrSttus`, 부문 매출은 원문 표에서 옵니다. 한때 47건이 전부 "재무제표"로
표시됐고, RA가 "이 배당성향 어디서?"라고 물으면 틀린 답을 하는 상태였습니다.
`Provenance.source_url`(우리가 호출한 곳)과 `verify_url`(사람이 열 링크)은
**다릅니다** — API 엔드포인트는 키가 없으면 안 열립니다.

### 2. 검산이 닫히지 않으면 쓰지 않는다

- 마진 브리지: Δ영업이익률 = (−Δ원가율) + (−Δ판관비율) — 항등식 (`finmodel/metrics.py`)
- 부문 매출 합계 ≈ 손익계산서 매출액, 비중 합 ≈ 100% (`finmodel/segments.py`)
- 부문 주석의 **총계 열** = 손익계산서 매출액·영업이익 (`finmodel/segment_profit.py`)
- 발행 − 자기주식 = 유통 (`data/kr/dart_reports.py`)
- EPS 교차검증: 재무제표 vs 배당공시 (`finmodel/valuation.py`)

**어긋나면 표시하고 넘어가지 말고 쓰지 마십시오.** 잘못 읽은 숫자가 리포트에
실리면 되돌릴 수 없습니다.

### 3. 투자의견·목표주가는 내지 않는다 (D4)

rating 어휘를 **부정문으로도** 쓰지 않습니다 — G0가 "제시하지 않는다"도 잡습니다.
"비중 확대"는 부문 비중을 말할 때도 금지입니다(overweight와 충돌).

대신 **논지**는 냅니다. 「관전 포인트」가 그 자리입니다.

### 4. 미검증 레인에는 숫자가 없다 (D31)

「0. 산업 배경」은 공시 밖 서술이라 **숫자가 하나라도 있으면 문단을 버립니다.**
연도도 막습니다. 리포트 전체는 계속 냅니다.

### 5. 부문 구분은 한 리포트에 하나만 (D33)

영업부문 주석(4.1)과 매출 표(2.2)는 분류가 다릅니다. 둘을 함께 실으면 같은
문서에서 「부문」이 두 뜻이 됩니다 — 실측: 삼성전자가 앞은 2개, 뒤는 4개였습니다.
주석이 있으면 그쪽만 남깁니다.

### 6. 뷰는 인물이 아니라 렌즈로 낸다 (D35)

실명 페르소나(버핏·애크먼 에이전트)는 **채택하지 않았습니다** — "버핏이라면
샀을 것"은 우회가 아니라 투자의견 그 자체이고(D4 위반), 살아 있는 인물에게
하지 않은 말을 귀속시킵니다.

렌즈는 **질문**이지 **판정**이 아닙니다. 지킬 것 셋:

- **1순위에 답하지 못하면 결론을 내지 못합니다.** 이 규칙이 상용구를 막습니다 —
  없을 때 초판은 3순위 관찰로 결론을 내 코스닥 다섯 곳이 글자까지 같은 문장을
  받았습니다.
- **부호 하나로 접지 마십시오.** 렌즈는 「주된 발견 + 단서 + 다음에 볼 것」으로
  남습니다. 초판은 부차적 관찰이 1순위 발견을 뒤집어 LG전자에서 정반대 문장이
  실렸습니다.
- **판독은 회사를 지목해야 합니다.** 부문명·출자처·최대주주 이름을 부르고 크기는
  슬롯이 물고 옵니다. 이름도 숫자도 없는 판독은 상용구입니다.
- **부정 판독이 없다는 것은 긍정이 아닙니다** — 삼성전자는 부문 자산을 아예
  공시하지 않는데 "자본은 버는 곳에 놓여 있다"가 나왔던 자리입니다.

### 7. 노트 본문의 숫자는 서버가 만든다 — 프런트가 다시 만들지 않는다 (D37)

화면은 `web/`의 Next.js지만 **본문 HTML은 여전히 `render/html.py`가 만듭니다.**
React는 그 문자열을 `dangerouslySetInnerHTML`로 그대로 주입하고
`element.dataset`을 읽어 팝오버만 붙입니다.

**React에서 숫자를 포맷하지 마십시오.** 레지스트리를 거치는 경로가 둘이 되는
순간 불변식 1이 화면에서 무너집니다. 차트도 같습니다 — 서버가 만든 인라인 SVG를
넣습니다.

그래서 `web/app/note.css`의 선택자는 **Tailwind 유틸리티로 바꿀 수 없습니다.**
서버가 내보내는 고정 클래스명(`.num`, `.num--estimate`, `.note blockquote`)과
정확히 맞아야 합니다.

**출처는 마우스와 키보드 양쪽으로 열립니다.** `render_html()`이 `.num`에
`role="button" tabindex="0"`을 붙이고 화면이 Enter·Space를 받습니다. 리팩터링
중에 이 속성을 떨어뜨리면 키보드 검토자에게는 **출처가 아예 사라집니다** —
테스트로 고정돼 있습니다(`test_number_is_keyboard_reachable`).

---

## 지금 상태 (전부 실측 확인)

| | |
|---|---|
| 배포 | https://ai-research-center-production.up.railway.app · 리전 **Asia Southeast(싱가포르)** |
| 인증 | **Supabase(Google) 또는 HTTP Basic** — 설정된 쪽을 쓴다 (D42). Basic은 Supabase 확인 후 삭제 |
| 저장소 | github.com/hongkevin/ai-research-center (**PUBLIC**) |
| 화면 | `web/` — Next.js 16 · Tailwind v4 · shadcn/ui · **정적 익스포트** (D37) · **단계 레일** (D39) · **보드** (D40) · **리뷰 루프** (D41) · **발간은 카드에서** (D43) |
| 테스트 | 751 통과 · ruff 클린 · eslint 클린 |
| CI | 린트 · 테스트 · **프런트 빌드** · Docker 빌드+헬스체크+화면서빙 · **키 패턴 검사** |
| 성능 | 결정론 3.0초 / LLM 30~35초 (US 리전 기준 실측 — 싱가포르 이전 후 미측정) |
| 비용 | 건당 ~$0.005 (실측 $0.0055, `gpt-5.6-luna`) |

로컬 실행: `cd web && npm run build` 후 `arc web` (기본 8000)
생성: `arc generate 214450 -y 2025 --llm`
화면 개발(핫 리로드): `web/README.md` 참조

### 검증에 쓰는 4종목

성격이 갈려서 회귀 확인에 좋습니다.

| 종목 | 성격 |
|---|---|
| `214450` 파마리서치 | 다부문(의약품·의료기기·화장품) · **주요계정 폴백** 경로 |
| `068760` 셀트리온제약 | 중첩 소계 표 · 인력이 직군 구분 · 모회사 지배 |
| `005930` 삼성전자 | 대형주 · 전체계정 · 우선주 있음 · **부문 손익 4개** |
| `000660` SK하이닉스 | 단일 부문 — 부문 손익이 **없는 것이 정상** |

부문 손익(D33)은 다부문 회사에서만 나오므로 셋을 더 씁니다.

| 종목 | 성격 |
|---|---|
| `066570` LG전자 | 6개 부문 · **적자 부문 있음** · 부문명 아래 약칭 행이 더 있다 |
| `011170` 롯데케미칼 | 단위 **천원** · 괄호 음수 · 전사 영업적자 |
| `097950` CJ제일제당 | 3개 부문 · 지역 표가 같은 주석에 섞여 있다 |

---

## 코드 지도

```
data/kr/dart.py           재무제표 (전체계정 → 주요계정 폴백, D20)
data/kr/dart_reports.py   정기보고서 주요정보 6종 (주식수·배당·감사·인력·지분·출자)
data/kr/dart_document.py  사업보고서 원문 (ZIP·표 파싱·<TE> 셀)
data/kr/dart_toc.py       공시 목차 → 수치마다 자기 절로 가는 뷰어 링크 (D44)
data/kr/news_filter.py    기사 거르기 — 날짜·제목·소음·중복 (D47)
ingest/convert.py         업로드 문서 → 마크다운 · 깨짐 탐지 (D48)
ingest/prior.py           직전 노트에서 추정치·차례 읽기 (D48)
finmodel/quarterly.py     분기 시계열 — 호출 4번으로 8분기 (D57)
render/docx.py            노트 → Word (표를 진짜 표로, D53)
render/xlsx.py            노트 → Excel (숫자가 숫자로, D56)
ingest/model_fill.py      남의 엑셀 모델에 채워 넣기 — 수식은 안 건드린다 (D62)
store/notes.py            노트 지문 · 직전 대비 변화 (D46)
data/kr/ksic.py           KSIC 코드 → 업종명 (중분류, 헤더의 「산업」)
finmodel/metrics.py       지표 13종 · 마진 브리지 · 논지 생성
finmodel/segments.py      부문별 매출 (원문에서 뽑고 매출액으로 검산, D28)
finmodel/segment_profit.py 부문별 손익 (IFRS 8 주석, 총계 열로 검산, D33)
finmodel/business.py      사업 이해 (사업의 개요·지분·자회사)
finmodel/valuation.py     BPS·ROE·역산 주가·선행 PER
finmodel/estimates.py     추정 + revision 추적
finmodel/backtest.py      기준선 오차 측정 (시점 정합성, D34)
finmodel/lenses.py        분석 렌즈 — 같은 숫자에 다른 질문 (D35)
store/cards.py            작업 중인 리포트 = 카드 · 칸 자동 판정 (D40)
llm/number_registry.py    ★ 불변식의 심장. 등록·치환·탐지·마스킹
llm/narrate.py            서술 생성 + 산업 서사 레인(별도 호출)
llm/josa.py               조사 교정 (치환 시점, D23)
llm/revise.py             코멘트로 한 섹션만 고쳐 쓴다 (D41)
verify/g0.py              발간 게이트 4종
pipeline/earnings_review.py  S1~S6 관통 + 진행 콜백
render/html.py            수치에 출처를 달아 HTML로
web/                      API · 인증 · 작업 큐(SSE) · 정적 파일 서빙
```

화면은 저장소 루트의 `web/`에 따로 있습니다 (D37).

```
web/app/page.tsx          작업대 (왼쪽 조작 · 가운데 노트 · 오른쪽 근거)
web/app/note.css          ★ 서버가 발행하는 고정 클래스. 이름을 바꾸면 깨진다
web/components/note/      본문 주입 + 수치 출처 팝오버
web/components/workbench/ 폼 · 회사 검색 · 근거 패널
  └ stage-rail.tsx        파이프라인 단계 레일 — 진행 표시와 같은 자리 (D39)
web/components/board/     작업 보드 — 카드가 칸에 놓인다 (D40)
web/components/note/section-editor.tsx  코멘트 · 직접 편집 → diff → 버전 (D41)
web/lib/api.ts            /api/* 클라이언트 (타입은 app.py의 ViewModel)
web/lib/use-generation.ts 생성 → SSE 진행 → 결과
```

**파이프라인은 열려 있습니다** (D39). `build_report`가 단계마다 `StageReport`를
남기고(`status`·`checks`·`registered`·`note`) 화면이 그걸 레일로 냅니다.
`absent`(정상 부재)와 `failed`(결함)를 **반드시 구분**하십시오 — 단일 부문 회사에
부문 손익이 없는 것은 정상입니다.

---

## 남은 고도화 (우선순위 순)

### ~~① 리전을 싱가포르로~~ — **완료** (2026-08-04, 대시보드에서 변경)

**`x-railway-edge` 헤더로 리전을 확인하지 마십시오.** 같은 클라이언트에서
이틀 간격으로 `sin1`(싱가포르) → `hnd1`(도쿄)로 바뀌었습니다. 이 값은 요청을
받은 **엣지 PoP**이지 컨테이너가 있는 리전이 아닙니다. 확인하려면 Railway
**Settings → Region**을 보십시오.

**이전 후 성능을 다시 재지 않았습니다** — 위 표의 3.0초/30~35초는 US 리전
기준입니다.

참고로 **LLM 쪽은 리전 이동의 수혜가 거의 없습니다.** OpenAI는
`api.openai.com` 단일 엔드포인트이고 Cloudflare 애니캐스트라 TLS는 가까운
PoP에서 끊기지만 추론은 미국에서 돕니다(실측: 한국에서 TLS 27ms, TTFB 188ms →
엣지 뒤로 ~160ms). 리포트당 LLM 호출은 2건뿐이라 왕복은 30초 중 1% 수준이고,
나머지는 토큰 생성 시간입니다. DART는 **작은 호출이 많아** 왕복이 지배적이라
이득이 컸던 것이고, 둘은 성질이 다릅니다.

### ~~② 부문별 이익 (Q10)~~ — **완료** (D33)

`finmodel/segment_profit.py`. IFRS 8 「영업부문」 주석에서 부문별 매출·영업이익을
뽑고 주석의 **기업 전체 총계 열**이 손익계산서와 맞는지로 검산합니다.
실측 4사 통과(오차 0.000000%), 단일 부문 3사 정확히 거부.

리포트가 새로 답하는 것: **매출 1위와 이익 1위가 다르다**(삼성전자 DX 6.8% vs
DS 19.1%). 전사 지표에는 이 사실이 나타나지 않습니다.

부문 **감가상각·자산**도 붙였습니다(Q12 해소, D33 보강). 삼성전자 DS는
영업이익률 19.1%인데 **EBITDA 마진 48.9%** — 상각 부담 29.8pp. DX는 2.4pp라
두 사업은 같은 종류의 수익성이 아닙니다. 자산은 공시하는 회사만 나옵니다(2/4).

### ③ 시세 연결 (Q7) — 밸류에이션이 참고치를 벗어남

`KRX_API_KEY`가 비어 있습니다([data.go.kr](https://www.data.go.kr) 무료).
지금 PER·PBR은 배당수익률에서 **역산한 앵커**라 특정일 종가가 아닙니다.

어댑터는 이미 있습니다(`data/kr/krx_price.py`). 키만 넣으면
`ValuationSet.is_implied=False` 경로로 갈 수 있고, 나아가 **PER 밴드**(SMIC이
목표주가 근거로 쓰는 것)를 그릴 수 있습니다.

### ~~④ 추정 백테스트 (Q8)~~ — **완료** (D34)

`finmodel/backtest.py` · `arc backtest <종목들> --start 2021 --end 2024`.
**우리 기준선의 성적을 처음으로 알게 됐습니다** — 활동 중인 코스닥 100곳 ×
4개년(n=337):

| | 오차 중앙값 | 편향 | ±20% 내 | 매출 대비 | 흑/적 방향 |
|---|---|---|---|---|---|
| 매출액 | **18.5%** | +6.5% | 53% | — | 100% |
| 영업이익 | 55.9% | +6.8% | 23% | **4.7%p** | 81% |
| 당기순이익 | 66.4% | +10.8% | 19% | 5.6%p | 81% |

**매출 기준선은 참고용으로 쓸 만하고, 이익 기준선은 아닙니다.** 5곳 중 1곳은
흑자·적자 방향조차 틀립니다. D24가 "예측이 아니라 출발점"이라 라벨링한 것이
과한 겸손이 아니었습니다.

주의할 점 둘:
- **평균을 대표값으로 쓰지 마십시오.** 첫 집계에서 매출 편향이 +118%였는데
  +20,852% 한 건이 만든 값이고 중앙값은 +6.6%였습니다.
- **corpCode 인덱스에는 상장폐지 회사가 남아 있습니다.** 그냥 뽑으면 표본의
  절반이 폐지 종목이고 커버리지가 47%로 나옵니다(실제 91%).

다음 → Q13: 이 숫자를 리포트 본문에 실을 것인가 (D12의 정합성 표시 3종).

### ⑤ 렌즈 (D35) — 코스닥 실측 완료, 동료 평가 대기

렌즈 셋: **자본**(1순위 ROE, 코스닥 100%) · **집중**(부문 매출, 82%) ·
**재현성**(마진 브리지, 55%). 코스닥 22곳 실측에서 **관점 충돌 73%**입니다
(초판 16%).

**대형주로 검증하고 일반화하지 마십시오.** 초판이 그래서 실패했습니다 —
자본수익률 렌즈가 요구한 부문 자산이 코스닥 25곳 중 **0곳**이었습니다.
회귀 4종목 중 셋이 대형주라 이 함정이 안 보입니다.

다음 후보는 **자산가치**(자산 대비 싼가, 그 자산은 진짜인가)이고, 시세 연결(③)
후에 붙이는 편이 낫습니다. 다만 **먼저 동료에게 보여주십시오.**

### ⑥ 계속할 관점 — **엔진은 이미 아는데 화면이 버린다**

D39·D40·D41·D43·D44가 전부 같은 모양이었습니다. 새로 계산한 게 아니라 **이미
있는 것을 표면으로 끌어올린 것**입니다:

| | 엔진이 알던 것 | 화면이 하던 것 |
|---|---|---|
| D39 | 산출물마다 `reconciled`·`note`·`unavailable` | 통째로 버림 |
| D40 | 생성물이 30분 뒤 사라짐 | 한 번에 끝내야 하는 폼 |
| D41 | `assembled` + 레지스트리를 들고 있음 | 읽기 전용 스냅샷 |
| D43 | 카드의 현재 본문 | 생성 직후 원본을 발간 |
| D44 | 공시가 전년 누적·목차 좌표·KSIC를 줌 | 안 읽음 |

**다음에 볼 곳** (같은 냄새가 나는 자리):

- `SegmentProfitSet.section_title` — 어느 주석에서 왔는지 아는데 화면에 없음
- `ValuationSet.eps_stmt` vs `eps_disclosed` — 교차검증을 하고도 결과를 안 보여줌
- `MetricSet.missing_labels` — 못 구한 지표를 아는데 「지표 13종」이라고만 씀
- `BusinessProfile.signals` — 뽑아 놓고 렌즈에만 쓰고 본문엔 안 냄
- 카드 목록에 **전 분기 대비 무엇이 달라졌는지** — 버전 이력은 있는데 비교가 없음

### ⑦ 사람이 해야 하는 것

- **RA 인터뷰 — 예정일(2026-08-03)이 지났습니다.** 진행했다면 답을
  [interview-ra.md](interview-ra.md) 「인터뷰 후」 절차대로 반영하십시오.
  D2·D4를 흔드는 답이 나왔다면 그게 최대 수확이고, decisions.md에 재검토
  항목으로 기록해야 합니다. **이게 지금 가장 값진 입력입니다** —
  [corpus/FINDINGS.md](../corpus/FINDINGS.md)가 결과 데이터로는 리포트 품질을
  정의할 수 없다고 결론 냈고, 그래서 남은 길이 사람에게 묻는 것뿐입니다.
- **동료 피드백** — "RA 리포트 같지 않다"는 지점이 벤치마크 추론보다 정확합니다.
- **Q9 (시나리오 가정 범위)** — 코드로 못 푸는 질문.

---

## 의도적으로 안 한 것 (다시 제안하지 마십시오)

| 항목 | 이유 |
|---|---|
| P×Q 드라이버 추정 (ASP·물량) | 제품별 단가·물량이 공시에 없다 |
| Peer 비교표 | 다종목 처리 + 업종 분류가 별건 |
| 3표 완전 모델 (BS·CF 추정) | 손익 추정을 먼저 실측 검증한 뒤 |
| 카피라이팅 제목 | 품질 편차 + 컴플라이언스 위험 |
| Vercel 배포 · 프런트/백 분리 | 서버리스라 `.arc-store`가 사라져 revision 추적이 죽는다. 화면이 Next.js가 된 뒤에도 **정적 익스포트를 FastAPI가 서빙**해 컨테이너 하나를 유지한다 (D37) — corpCode 캐시·LLM 예산도 같은 프로세스에 있다 |

---

## 함정 (실제로 밟은 것들)

- **`.env.example`에 진짜 키** — 두 번 일어났습니다. `git config core.hooksPath .githooks`로
  훅을 설치하십시오. CI는 푸시 후라 늦습니다.
- **워커 1개 전제** — corpCode 캐시·LLM 예산·작업 큐가 전부 프로세스 메모리에
  있습니다. 늘리려면 Redis로 옮겨야 합니다.
- **리포트 템플릿이 wheel에 없음** — `src/arc` 밖이라 `ARC_TEMPLATE_DIR`로 지정합니다.
- **한글 정규식에 `\b`를 쓰지 마십시오** — 한글은 유니코드 단어문자라 "25배를"에서
  경계가 없습니다. G0에 구멍이 났던 자리입니다.
- **DART 소형주는 연결재무제표가 없습니다** — CFS→OFS→주요계정 폴백이 이미 있습니다.
- **재무제표 컬럼 이름이 보고서 종류마다 다릅니다** (D44). 분기·반기 **손익**은
  `thstrm_add_amount`/`frmtrm_add_amount`(누적)이고 `bfefrmtrm_amount`가 아예
  없습니다. 사업보고서 컬럼만 읽으면 **분기 노트의 전년 대비가 통째로 빕니다** —
  차트가 비어 있을 때 차트를 의심하기 전에 파서를 보십시오. 재무상태표는 분기에도
  `frmtrm_amount`라 규칙이 반대입니다.
- **`(("a", "b"))`는 튜플이 아니라 문자열입니다.** 쉼표를 빼먹어 목차 라우팅이
  "주" 한 글자로 매칭됐습니다 (D44). 문자열 튜플을 순회하는 코드에서 조용히 틀립니다.
- **출자처 법인명에 각주가 붙어 옵니다** (`… Berhad (*6)`). G0가 미등록 숫자로
  막았고 그건 옳은 판정입니다 — 게이트를 풀지 말고 이름을 고치십시오.
- **「부정이 없다」를 「긍정」으로 세지 마십시오.** 삼성전자는 부문 자산을 아예
  공시하지 않는데 렌즈가 "자본은 버는 곳에 놓여 있다"고 썼습니다. 근거의 부재와
  긍정적 근거는 다릅니다.
- **적자에서는 부등호의 뜻이 뒤집힙니다.** ROE > ROA를 레버리지 신호로 읽는 규칙이
  롯데케미칼(적자)에서 반대로 발화했습니다. 비율 비교는 부호를 먼저 확인하십시오.
- **같은 함정을 세 번 밟았습니다**: 「가장 크게 움직인 것」을 고를 때 비율이 아니라
  **기여도(크기)**로 골라야 합니다. 매출 비중 2%짜리가 10pp 흔들려도 전사에는
  영향이 없습니다 (D28·D33·D33 보강).
- **정적 파일 마운트는 반드시 `/api/*` 뒤에 등록하십시오.** `/`에 붙기 때문에
  먼저 걸리면 API 요청을 정적 파일 조회가 가로챕니다 (`web/app.py` 맨 끝).
- **shadcn은 Radix가 아니라 Base UI를 씁니다** (`base-nova` 스타일). 컴포넌트
  API가 다릅니다. `web/AGENTS.md`가 "이 버전은 네가 아는 Next.js가 아니다,
  `node_modules/next/dist/docs/`를 읽어라"라고 경고하는데 **맞는 말이고 지우지
  마십시오** — 이 사실을 거기서 확인했습니다.
- **`--header-h`를 헤더 여백과 함께 고치십시오.** 좌우 열이 sticky로 이 값만큼
  내려붙습니다. 어긋나면 스크롤할 때 열이 헤더 밑으로 파고듭니다.
- **네이버 검색 API는 2026-07-31부터 NAVER API HUB(NCP)에서만 발급됩니다.**
  주소·인증 헤더가 다릅니다(`NAVER_API_MODE`, 기본 `hub`). 옛 주소로 때리면
  401 `errorCode 024`가 나는데, **키가 틀린 것처럼 보입니다** — 실제로 한 번
  그렇게 오진했습니다.
- **DART `corp_name`은 영문을 한글로 음차합니다** (`에이치디현대중공업(주)`).
  기사·시세처럼 바깥과 맞출 때는 `stock_name`(`HD현대중공업`)을 쓰십시오.
- **백그라운드 명령을 `| tail`로 파이프하면 실패가 숨습니다.** `docker build`가
  `command not found`로 죽었는데 파이프라인 종료코드가 `tail` 것이라 0으로
  보고됐습니다. 성공했다고 착각한 채 넘어갔던 자리입니다.

---

## 다음 세션 시작하는 법

```
docs/HANDOFF.md 와 docs/decisions.md 읽고 이어가자.
```

세부는 이 순서로 보면 됩니다:
[decisions.md](decisions.md) D1~D62 →
[corpus/FINDINGS.md](../corpus/FINDINGS.md)(왜 사람에게 묻는 쪽으로 돌았는가) →
[research/01-benchmark-smic.md](research/01-benchmark-smic.md)(SMIC 갭 분석) →
[DEPLOY.md](DEPLOY.md)(배포·제약)

화면을 만질 거라면 [web/README.md](../web/README.md)를 먼저 보십시오 —
`note.css`를 왜 Tailwind로 바꿀 수 없는지가 거기 있습니다.
