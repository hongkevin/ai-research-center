"""finmodel — 결정적 계산 레이어 (ARCHITECTURE.md §4.1 S3, §4.2).

역할: 실적 추정, 멀티플 밴드, 시나리오 3개(보수/기준/낙관), 감도표 계산.
**모든 수치는 이 모듈의 결정적 코드가 계산한다** — LLM은 가정값(성장률 등)을
JSON으로 제안만 하고, 계산은 전부 여기서 수행해 Number Registry에 등록한다.

불변식 (§3 D4): 단일 목표주가·투자의견을 계산·출력하지 않는다.
적정가치 범위 + 감도표 + 산출 산식 공개 형태만 허용.

TODO(3~4주차):
  - estimate.py : 실적 추정 (가정 JSON → 추정 재무)
  - multiples.py: 과거 밴드·피어 멀티플 계산 (컨센서스 없이 자체 밸류에이션)
  - scenario.py : 보수/기준/낙관 시나리오 + 감도표 생성
"""
